name: wb-review-bot
type: python
python_version: "3.8"
from_directory: .
run_command: python check_port.py && gunicorn -c gunicorn.conf.py main:app
build_command: pip install -r requirements.txt
port: 8080
